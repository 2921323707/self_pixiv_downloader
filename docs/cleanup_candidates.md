# Cleanup Candidates

Generated on 2026-05-23 after Phase 6B. Last reviewed on 2026-05-27 after user-approved cleanup and design demo archival.

Purpose: review files/directories that are likely no longer needed after the current downloader-first milestone. No deletion should happen until the user explicitly approves.

## Current Anchored State

- Default visual theme: Demo B / `cyan-studio`.
- Core backend path: `src/backend`.
- Downloader-first implementation exists and passes local tests.
- Phase 4C settings-backed single download runtime exists and passes local tests.
- Phase 4D Gallery file preview exists and passes local tests.
- Phase 4E Gallery hard-delete file/index cleanup exists and passes local tests.
- Phase 5A Author batch download exists and passes local tests.
- Phase 5B Bookmarks batch download exists and passes local tests.
- Phase 6A Smart Retrieval parse exists and passes local tests.
- Phase 6B Smart Retrieval tag-search download exists and passes local tests.
- Tauri Desktop MVP, GitHub `v1.1.1` mature delivery release, ad-hoc signed `.dmg` loop, Gallery preview stability fix, and Pixiv login-cookie
  refresh implementation are documented.
- Pixiv login-cookie feasibility probe temporary code has been removed; the formal Tauri command and Settings
  Refresh flow are now in place and live-verified by the user.
- Live Pixiv single-work smoke passed for work `144920810`.
- Manual browser single-work download passed on 2026-05-22.
- Manual browser Author Batch and Bookmarks Batch downloads passed on 2026-05-22.
- Repeatable test entrypoints exist under `tests/`.
- 2026-05-27 cleanup removed the old project-local `output/` directory after storage defaults were unified.
- Theme demo references now live under `docs/design/theme-reference/`.

## Cleaned On 2026-05-27 After User Approval

| Path | Result | Notes |
| --- | --- | --- |
| `src/frontend/.next/` | Deleted | Next.js build/dev cache; reproducible. |
| `src/frontend/out/` | Deleted | Next static export; regenerated by Tauri/Next build. |
| `tauri-app/src-tauri/target/` | Deleted | Tauri/Rust build output; release artifacts must be rebuilt when needed. |
| `tauri-app/src-tauri/gen/` | Deleted | Tauri generated schema/config output; reproducible. |
| `tauri-app/.DS_Store` | Deleted | macOS Finder metadata. |
| `tauri-app/src-tauri/.DS_Store` | Deleted | macOS Finder metadata. |
| `src/frontend/tsconfig.tsbuildinfo` | Deleted if present | TypeScript incremental build metadata; reproducible. |
| `output/` | Deleted | Local downloads and SQLite index were intentionally reset by user request; this is no longer the default download root. |
| `docs/assets/readme-hero.svg` | Deleted | No current references found. |
| `static/innerFiles/image.png` | Deleted | No current references found. |
| `demoPageDisplay/` | Moved | Archived as `docs/design/theme-reference/`. |

## Remaining Recommended To Delete

No current ignored build-cache or `.DS_Store` cleanup target was found after the 2026-05-27 cleanup pass.

## Should Keep Or Review Before Deleting

| Path | Reason |
| --- | --- |
| `~/Downloads/Pixiv Platform/` | Shared default Web/App download root. Do not delete automatically because it may contain user downloads and SQLite state. |

## Optional To Delete Or Archive

| Path | Size | Reason | Recommendation |
| --- | ---: | --- | --- |
| `docs/design/theme-reference/demo_B.html` | 12K | Selected theme reference. | Keep as design archive. |
| `docs/design/theme-reference/sakura-light.html` | 3.8K | Alternate retained theme reference. | Keep as design archive while `sakura-light` remains a supported theme option. |

Note: `demo_A.html`, `demo_C.html`, and `/private/tmp/pixiv_platform_phase5a_verify.sqlite3` are already absent in the current workspace.

## Should Keep

| Path | Reason |
| --- | --- |
| `.gitignore` | Prevents build artifacts, env files, `.DS_Store`, and local downloads from entering git. |
| `docs/product_requirements.md` | Original PRD source. |
| `docs/specs/` | Current spec-coding source of truth. |
| `docs/cleanup_candidates.md` | This review list until cleanup is complete. |
| `src/backend/Cargo.toml` | Backend crate manifest. |
| `src/backend/Cargo.lock` | Should be committed for reproducible application builds. |
| `src/backend/src/` | Current downloader core implementation. |
| `tests/run_local.sh` | Deterministic local test entrypoint. |
| `tests/unit/backend_unit.sh` | Backend unit test entrypoint. |
| `tests/e2e/live_single_download.sh` | Opt-in live Pixiv E2E entrypoint. |
| `tests/live/README.md` | Documents the successful live smoke result without credentials. |
| `tests/fixtures/README.md` and `tests/smoke/README.md` | Placeholders for planned test layers. |

Already cleaned after user approval:

- `/private/tmp/pixiv_platform_manual/`
- 2026-05-27 cleanup batch listed above
