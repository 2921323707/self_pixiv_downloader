# Design Archive

This directory stores visual references that are not runtime dependencies.

## Theme Reference

`theme-reference/` contains the selected Demo B direction for the default `cyan-studio` theme and the retained `sakura-light` comparison reference.

These files are preserved as design history. Runtime theme implementation lives in `src/frontend/app/globals.css`, `src/frontend/components/AppShell.tsx`, and the Settings theme controls.
