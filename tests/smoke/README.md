# Smoke Tests

Reserved for fast cross-module smoke checks after API and frontend scaffolds exist.

Current state:

- Use `tests/run_local.sh` for deterministic local checks.
- Use `tests/e2e/live_single_download.sh` for opt-in Pixiv live download verification.
